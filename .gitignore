# 설치된 라이브러리 (용량이 매우 큼)
/node_modules

# 빌드 결과물 (배포 시 자동으로 생성됨)
/dist
/dist-server

# 운영체제별 시스템 파일
.DS_Store
Thumbs.db

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 환경 변수 파일 (가장 중요! 비밀 정보 포함)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local